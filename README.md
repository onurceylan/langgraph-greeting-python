# LangGraph Greeting Agent (Python)

Simple greeting agent using LangGraph without LLM.

## Setup
```bash
# Install uv - if it is not working use in bash or pip
curl -LsSf https://astral.sh/uv/install.sh | sh

# Install dependencies
uv sync